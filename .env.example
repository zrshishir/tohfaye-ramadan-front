# API base URL — must include the /api suffix.
# Production:
VITE_BASE_URL=https://prayerpulse.tazqiah.com/api
# Local development against a Laravel backend, e.g.:
# VITE_BASE_URL=http://127.0.0.1:8000/api

# The Google Maps key is no longer used by the app. Reverse geocoding goes through
# GET /api/geocode so the key stays on the server. Set GOOGLE_MAPS_KEY there instead.
